// a simple for each callback function
// cb fuctions has always 3 Elements, so
// immmer 3 :  valu , index, object )

nameArray = ["Susi", "Trudl", "Clothhilde"];

nameArray.forEach(name => {
    console.log(name, " is cool");
});